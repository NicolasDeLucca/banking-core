spring:
  datasource:
    url: jdbc:h2:mem:bankingcore-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: "test-only-secret-key-at-least-32-characters-long"
    expiration-seconds: 3600
  admin:
    email: admin@bankingcore.local
    password: TestOnlyAdmin123!
